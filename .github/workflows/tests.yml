# 자동 테스트 — PR 생성·갱신 시와 main push 시 빠른 층 테스트를 실행한다.
# 느린 층(-m slow)은 로컬 데이터(시세 캐시·데이터셋)가 필요해 CI에서는 제외
# (해당 테스트들은 데이터 없는 환경에서 자동 skip 되도록 작성돼 있음).
name: tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.lock

      - name: 의존성 설치 (lock 고정 버전)
        run: pip install -r requirements.lock

      - name: 빠른 층 테스트
        env:
          # JWT 인증 도입 후 auth.py가 모듈 로드 시점에 이 값을 요구함.
          # 실제 배포 시크릿과 무관한 테스트 전용 값 — CI에서만 쓰임.
          JWT_SECRET_KEY: test-only-secret-not-used-in-production-ci
        run: pytest -q