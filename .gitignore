.venv/
__pycache__/
*.pyc
.DS_Store
.env

# 생성물 / 로컬 DB
reports/
*.db

# synthetic_data pykrx 시세 캐시 (재생성 가능한 파생 데이터)
synthetic_data/.cache/
# backend 분석용 시세 캐시 (price_cache.py — 재생성 가능)
backend/.price_cache/
# 7-5 다중 시드 데이터셋 산출물 — generate_dataset.py로 완전 재현 가능(용량상 미커밋, 팀 드라이브 전달)
dataset/

# 루트 canonical 산출물(시드 7) — synthetic_data.main 1회 실행으로 완전 재현(정책 통일: 산출 데이터 미커밋)
synthetic_trades.csv
synthetic_labels.csv
synthetic_meta.csv
synthetic_trade_labels.csv

# 3계층 ML 파생물 — ml.prepare / ml.train_gru로 재생성 가능
ml/cache/
ml/artifacts/

# 작업 프로세스 로그 — 로컬 전용
PROCESS.md
