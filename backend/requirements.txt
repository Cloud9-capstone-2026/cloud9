fastapi
uvicorn
sqlalchemy
psycopg2-binary
# 수치 라이브러리 버전 고정 (2026-09-02): 배포가 미고정 설치라 pandas 3.0으로 올라가며
# 분석이 AttributeError로 전멸(빈 프레임 concat dtype 규칙 변경). requirements.lock과 동일 버전.
pandas==2.3.3
numpy==2.4.4
python-multipart
python-dotenv
scipy==1.17.1
openpyxl
# CSV 업로드 자동 매핑(단계 5) — LLM 호출 + HTML 위장 .xls(키움 등) 파싱
google-genai
lxml
# 3계층 GRU 추론용 torch — CPU 휠 강제 (아래 extra-index-url을 pip이 requirements
# 안에서 직접 읽으므로 Railway 등 자동 빌드에서도 CUDA 포함 기본 휠(수 GB) 대신
# CPU 휠이 설치된다. 로컬 수동 설치도 pip install -r 로 동일.)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
alembic
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
email-validator
PyYAML
slowapi
# 소셜로그인(2026-08-18) — 아래 3개 모두 requirements.lock에 이미 존재
# (다른 파트 의존성으로 유입됨)하지만 backend가 직접/간접 import하므로
# 명시적으로 추가. requests는 google-auth 내부(google.auth.transport.requests)가
# 요구하는 숨은 의존성 — httpx만으로는 대체 안 됨(google-auth 라이브러리
# 자체가 requests를 직접 import하는 구조라서). requirements.lock만 쓰던
# 로컬/CI 환경에서는 다른 패키지가 requests를 끌고 와서 문제가 안 드러났다가,
# EC2 배포(deploy.yml이 backend/requirements.txt로만 설치)에서
# ModuleNotFoundError로 처음 드러남 (2026-08-18).
google-auth==2.53.0
httpx
requests
# 시세 캐시(parquet) — backend/price_cache.py, synthetic_data/market/krx_api.py 가 pandas.to_parquet/read_parquet 사용
pyarrow
# AWS Parameter Store에서 진짜 비밀값(DATABASE_URL/JWT_SECRET_KEY/GMAIL_APP_PASSWORD)을
# 읽어오는 용도 (2026-08-26, .env 평문 저장에서 전환). EC2에 붙은 IAM 역할로
# 별도 액세스키 없이 인증됨 — 로컬 개발 환경엔 이 역할이 없어 조용히 실패하고
# .env로 폴백(secrets_loader.py 참고).
boto3==1.43.80
apscheduler